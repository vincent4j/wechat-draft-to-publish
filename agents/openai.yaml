interface:
  display_name: "WeChat Draft to Publish"
  short_description: "支持分步确认或一次确认自动跑完，并清理无用中间产物"
  default_prompt: "使用 $wechat-draft-to-publish 处理这份公众号草稿，先让我选择分步确认或自动完成；自动模式在我确认开始后连续跑完，每一步反馈进度，最后清理无用中间产物。"

policy:
  allow_implicit_invocation: true
