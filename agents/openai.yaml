interface:
  display_name: "WeChat Draft to Publish"
  short_description: "支持按需生成完整或局部公众号发布包，完成配图、封面、排版与文案"
  default_prompt: "使用 $wechat-draft-to-publish 处理这份公众号草稿。先根据我的自然语言要求识别需要的产物和执行范围；支持直接完成或在关键结果处边做边看，结束时告诉我实际做了什么。"

policy:
  allow_implicit_invocation: true
