---
name: wechat-draft-to-publish
description: 将 Markdown 文章草稿按用户需要完成去除 AI 味、正文配图、公众号封面、公众号排版、120 字以内的公众号摘要和 100 个汉字以内的朋友圈转发文案，支持只做一项、指定范围或生成完整发布包，也支持直接完成或在关键结果处边做边看。用户要求处理公众号草稿、完整生成公众号发布稿、给文章配图、制作封面、排版、生成摘要或朋友圈转发文案时使用。
---

# 草稿到公众号成稿

把一份文章草稿处理成可复制到微信公众号编辑器的图文成稿。编排现有 Skills，不复制它们的实现。

开始前读取：

- `references/defaults.md`：各步骤的默认参数和产物规则。
- `references/receipts.md`：开始话术、步骤回执和确认格式。

## 核心规则

1. 支持“直接完成”和“边做边看”两种模式。用户已经明确执行方式或具体产物时直接执行；只有“处理一下公众号文章”这类范围和方式都不清楚的请求才让用户选择。
2. 先识别用户要得到的产物，再换算成执行步骤。支持只做一项、执行一段范围、选择多个指定产物或生成完整发布包，不要求用户理解步骤编号。
3. 每一步完成后都给出回执。“直接完成”给出进度回执后继续；“边做边看”只在正文、配图、封面和排版这些需要主观判断的实际结果完成后暂停，摘要、朋友圈文案和机械校验不单独暂停。
4. 用户明确说“直接完成”“自动”“全部处理”或清楚指定单项任务，即视为对本次执行范围和默认生成参数的明确授权。展示执行摘要后立即开始，不再等待第二次“开始”确认。
5. 直接完成遇到输入缺失、子 Skill 不可用、默认配置失效、需要偏离既定方案或存在安全风险时暂停，不猜测。
6. 保持向后兼容：`自动 N` 表示直接完成第 N 至第 6 步；`分步 N` 或单独数字表示边做边看第 N 至第 6 步。步骤编号是兼容入口，不是主要交互方式。
7. 用户要求修改当前结果时，只重做当前步骤；直接完成运行中收到修改要求时先停止推进并处理修改。
8. 重新执行较早步骤后，说明哪些已有后续产物已经过期；只有这些步骤仍在本次执行范围内时才重新生成，不擅自扩大范围。
9. 当前用户要求优先于工作流默认参数；工作流默认参数优先于子 Skill 的偏好文件、首次配置和自动默认值。
10. 每次结束都必须给出完整执行回执，明确实际完成、明确跳过、生成文件、检查结果和清理结果；不得把计划执行但未成功的内容写成已完成。
11. 只处理用户指定的文章及工作流明确生成的产物，不改动无关文件。

## 子 Skill 配置继承（硬约束）

调用任何子 Skill 前，必须先把当前用户要求与 `references/defaults.md` 合并成当次调用参数。

1. 只要工作流已经提供某项参数，就把它视为已解析配置，直接传给子 Skill。
2. 工作流默认参数足够完成当前步骤时，**禁止执行子 Skill 的首次配置流程**，不得再询问水印、默认风格、输出目录、偏好保存范围等问题。
3. 不得为了满足子 Skill 的 `EXTEND.md` 检查而创建或修改偏好文件；本工作流的参数只对当次文章处理生效。
4. 缺少子 Skill 偏好文件不算配置失效。只有当前用户要求和工作流默认参数都没有提供某个必需值，并且无法安全继续时，才暂停并只询问缺失项。
5. 子 Skill 自身的首次配置、偏好文件和自动默认值不得覆盖已经解析的工作流参数。

这组约束同时适用于“直接完成”和“边做边看”。“边做边看”使用已经解析的配置直接生成，在看到实际配图或封面后确认结果；不得把首次偏好配置或额外的生成前方案确认混入关键结果确认。

## 识别执行范围

先从用户自然语言中提取目标产物和排除项，再形成当次执行清单：

- “全部处理”“完整发布稿”表示执行第 1 至第 6 步。
- “只做封面”“只生成摘要”表示只执行对应步骤，完成后停止。
- “从配图做到排版”表示执行第 2 至第 4 步。
- “配图、封面和排版都做，正文别动”表示执行第 2、3、4 步，明确跳过第 1、5、6 步。
- 用户列出多个不连续产物时，只执行列出的步骤，不自动补齐中间步骤。

执行某一步需要输入文件时，优先使用用户指定版本；否则按该步骤的来源优先级选择已有产物。缺少安全执行所必需的输入时，只询问缺失项。不得因为某一步通常位于后面，就擅自执行用户没有要求的前置步骤。

如果用户只提供文章并笼统地说“处理成公众号文章”，范围默认为完整发布包，但执行方式仍不清楚，此时才展示双模式选择。用户明确说“只做……”或直接点名具体产物时，按“直接完成”执行该范围；用户说“先给我看”“每个关键结果确认”时，按“边做边看”执行。

## 执行模式

### 边做边看

1. 根据用户目标确定执行清单。
2. 展示执行摘要，列出实际执行和明确跳过的步骤；摘要发出后开始执行，不增加第二次开始确认。
3. 执行清单中的当前步骤并给出回执。
4. 当前步骤属于第 1 至第 4 步时，展示实际结果并暂停，等待用户继续或提出修改；不增加单独的生成前方案确认。
5. 第 5、6 步及机械校验连续完成，不设置无意义的确认门。
6. 到达执行范围末尾后直接给出完整执行回执。

### 直接完成

1. 用户明确选择直接完成、自动处理、全部处理或清楚指定单项任务时即完成授权。
2. 展示执行摘要：文章路径、实际执行步骤、明确跳过步骤、关键默认参数、预计产物和适用的清理范围；摘要是执行状态说明，不是确认门。
3. 摘要发出后立即连续执行当次清单；每一步完成后使用进度回执反馈，但不暂停，也不自动扩展到第 6 步。
4. 正文配图和封面按 `references/defaults.md` 以及已保存的项目偏好直接生成，不再单独确认方案。
5. 只有满足“收尾清理”的前提时才清理，随后完成发布包一致性检查并给出完整执行回执。

## 开始工作流

先确认文章路径。找不到文章或存在多个可能文件时，询问用户，不猜测。

范围和执行方式都不明确时，使用 `references/receipts.md` 中的双模式开始话术。用户可直接回复：

- `直接完成`：按默认方案生成完整发布包，中间持续反馈但不暂停。
- `边做边看`：在正文、配图、封面和排版等关键结果完成后确认。

同时接受“只排版”“从配图做到排版”等自然语言范围。`自动 N`、`分步 N` 和单独数字继续按兼容规则处理。

用户选择直接完成后，使用执行摘要并立即开始当次执行清单；不得再要求用户回复 `开始`。

## 第 1 步：去除 AI 味

使用 `$humanizer-zh`：

1. 保留原意、事实、标题结构、链接和图片引用。
2. 处理生硬表达、重复内容、模板化句式和明显的 AI 写作痕迹。
3. 不因为本工作流额外增加或删除 Emoji。
4. 默认不覆盖原稿，输出带 `_去除AI味` 后缀的 Markdown 文件。
5. 完成后检查标题层级、链接和图片引用是否仍然完整。

完成后按当前模式回执：边做边看展示实际结果并等待继续；直接完成发送进度回执并进入执行清单中的下一项。若已到范围末尾，给出完整执行回执。

## 第 2 步：正文配图

使用 `$baoyu-article-illustrator`：

1. 读取 `references/defaults.md` 中的正文配图参数。
2. 按“子 Skill 配置继承”解析并显式传入预设、密度、配色、语言、水印、输出目录、图片后端和批量大小；不得因为缺少 `EXTEND.md` 触发首次配置。
3. 先分析文章结构，确定配图位置、数量和本次生成参数。
4. 两种模式都按已解析配置直接生成；边做边看在实际图片生成后确认结果，不增加生成前方案确认。
5. 将图片保存到文章同目录的 `assets/`。
6. 使用简单文件名：`section-01.png`、`section-02.png`；同一章节多图时使用 `section-02-1.png`。
7. 将相对图片路径插入 Markdown，生成带 `_配图版` 后缀的文件。
8. 检查图片文件存在、Markdown 引用有效、图片位置与正文语义匹配。

完成后按当前模式回执：边做边看展示实际图片和配图版并等待继续；直接完成发送进度回执并进入执行清单中的下一项。若已到范围末尾，给出完整执行回执。

## 第 3 步：封面制作

使用 `$baoyu-cover-image`：

1. 读取 `references/defaults.md` 中的封面参数。
2. 按“子 Skill 配置继承”解析并显式传入类型、配色、渲染方式、文字方案、情绪、比例、语言、水印、输出路径、图片后端和风格参考；不得因为缺少偏好文件触发首次配置。
3. 根据文章标题、核心冲突和目标读者形成封面方案。
4. 两种模式都按已解析配置直接生成；边做边看在实际封面生成后确认结果，不增加生成前方案确认。
5. 将封面保存为文章同目录下的 `assets/cover.png`。
6. 检查画面主题、文字清晰度、缩略图可读性和宽高比。

完成后按当前模式回执：边做边看展示实际封面并等待继续；直接完成发送进度回执并进入执行清单中的下一项。若已到范围末尾，给出完整执行回执。

## 第 4 步：文章排版

使用 `$gzh-design`：

1. 读取 `references/defaults.md` 中的排版参数。
2. 优先使用配图版 Markdown；用户指定其他版本时使用指定文件。
3. 生成纯净 HTML 和一键复制预览版 HTML。
4. 按排版默认参数决定是否生成顶部卡片、目录、作者签名和互动卡；章节序号连续且与正文一致。
5. 纯净 HTML 保留相对图片路径。
6. 一键复制预览版将本地图片内嵌，确保复制到微信公众号编辑器时能够携带图片。
7. 检查标题数量、图片引用、图片内嵌状态和排版校验结果。

完成并校验后按当前模式回执：边做边看展示预览结果并等待继续；直接完成发送进度回执并进入执行清单中的下一项。若已到范围末尾，给出完整执行回执。此时不宣称文章已经发布。

## 第 5 步：公众号摘要

直接基于最终正文生成可粘贴到微信公众号后台的文章摘要，不调用额外子 Skill：

1. 用户指定输入版本时使用该文件；否则依次优先使用 `_配图版.md`、`_去除AI味.md`、原稿 Markdown。
2. 摘要必须忠于正文，说明文章讨论什么、读者能获得什么；不得补充正文没有的事实或夸大承诺。
3. 只输出一个自然段，不带标题、列表、Markdown 标记、Emoji、话题标签或链接。
4. 去除首尾空白后不超过 120 个字符；中文、标点、英文字母和数字都计入长度。
5. 保存为文章目录下的 `{原稿}_公众号摘要.md`，文件中只保留摘要正文。
6. 运行 `python3 <SKILL_ROOT>/scripts/validate_summary.py <摘要文件>`，必须通过长度和格式校验。

完成后不设置单独确认门；如果执行清单中还有步骤则继续，否则给出完整执行回执。此时只有满足收尾清理全部前提时才进入清理。

## 第 6 步：朋友圈转发文案

直接基于最终正文生成用户转发公众号文章到朋友圈时使用的配文，不调用额外子 Skill：

1. 用户指定输入版本时使用该文件；否则依次优先使用 `_配图版.md`、`_去除AI味.md`、原稿 Markdown。
2. 文案必须忠于正文，用自然、具体的分享口吻说明这篇文章为什么值得点开；不得补充正文没有的事实、虚构读者反馈或夸大承诺。
3. 文案应与公众号摘要有明显区别：摘要负责概括内容，朋友圈文案负责表达分享动机并给出一个阅读钩子。
4. 开头直接进入问题、观察或观点；不得以文章标题、系列名称、“第 N 篇”或“这篇文章讲了……”作为开场。
5. 只输出一个自然段，不带标题、列表、Markdown 标记、链接或话题标签；默认不使用 Emoji，用户明确要求时除外。
6. 去除首尾空白后不超过 100 个汉字。只统计汉字；标点、空格、英文字母和数字不计入汉字数。
7. 保存为文章目录下的 `{原稿}_朋友圈文案.md`，文件中只保留文案正文。
8. 运行 `python3 <SKILL_ROOT>/scripts/validate_moments_copy.py <朋友圈文案文件>`，必须通过汉字数和格式校验。

完成并校验后执行发布包一致性检查并给出完整执行回执。只有第 4、5、6 步都在本次执行范围内且产物全部验证通过，或用户明确要求清理时，才执行“收尾清理”；只执行第 6 步时只交付朋友圈文案，不清理其它资源。

## 发布包一致性检查

到达本次执行范围末尾后，检查本次实际生成的产物以及同一发布包中仍有效的已有产物：

1. 以本次最终使用的 Markdown 正文为权威来源。
2. 检查封面、正文配图、公众号摘要和朋友圈文案是否围绕同一篇文章的标题、核心观点和目标读者，没有使用过期正文或跑到其它主题。
3. 检查公众号摘要与朋友圈文案分工明确，不是同一句话的改写。
4. 重新执行正文、配图或排版后，如果未纳入本次范围的后续产物可能已经过期，在回执中明确列出，不擅自重做。
5. 只检查本次范围内生成或已有且可验证的产物；缺少某项不视为失败，也不得为了凑齐发布包扩大执行范围。

## 收尾清理

第 4、5、6 步都在本次执行范围内，纯净 HTML、预览版、图片、摘要和朋友圈文案全部校验通过后执行；用户明确要求清理时也可单独执行。“直接完成”和“边做边看”都启用。只做单项或局部范围时默认不清理，避免产生范围外副作用。

1. 先运行预览模式，检查待删除清单：

   ```bash
   python3 <SKILL_ROOT>/scripts/cleanup_artifacts.py <文章目录>
   ```

2. 清单只允许包含 `references/defaults.md` 中登记的已知中间产物；确认没有正式文件后执行：

   ```bash
   python3 <SKILL_ROOT>/scripts/cleanup_artifacts.py <文章目录> --apply
   ```

3. 必须保留：
   - 原稿 Markdown。
   - `_去除AI味.md`。
   - `_配图版.md`。
   - 纯净排版 HTML。
   - 一键复制预览版 HTML。
   - `_公众号摘要.md`。
   - `_朋友圈文案.md`。
   - `assets/cover.png`。
   - 被保留 Markdown 或纯净 HTML 实际引用的正文图片。
4. 只删除：
   - 已被 `cover.png` 取代的 `assets/cover-*` 候选图。
   - 未被任何保留文档引用的 `assets/section-*` 图片。
   - `assets/outline.md`、提示词文件、未引用的参考图副本和专用临时目录。
5. 不删除文章根目录中的任何 Markdown 或 HTML。遇到不符合已知命名规则的文件时保留，并在最终回执中列出。
6. 最终回执列出摘要与朋友圈文案字符数、实际删除数量、保留的正式资源和未识别但保留的文件。

## 缺少子 Skill

如果 `$humanizer-zh`、`$baoyu-article-illustrator`、`$baoyu-cover-image` 或 `$gzh-design` 不可用：

1. 明确列出缺少的 Skill。
2. 不自动下载、安装或替换实现。
3. 保留已完成产物，等待用户决定。

## 维护校验

修改本 Skill 的配置继承、交互或默认参数后，运行：

```bash
python3 scripts/validate_workflow_contract.py
```

必须通过后才算完成修改。
