---
name: wechat-draft-to-publish
description: 将 Markdown 文章草稿依次完成去除 AI 味、正文配图、公众号封面和公众号排版，生成达到微信公众号发布要求的图文成稿。用户要求处理公众号草稿、给文章自动配图、制作封面、生成公众号排版稿，或希望从上述任一步骤开始时使用。
---

# 草稿到公众号成稿

把一份文章草稿处理成可复制到微信公众号编辑器的图文成稿。编排现有 Skills，不复制它们的实现。

开始前读取：

- `references/defaults.md`：各步骤的默认参数和产物规则。
- `references/receipts.md`：开始话术、步骤回执和确认格式。

## 核心规则

1. 每一步执行完成后给出回执，并暂停等待用户确认。
2. 用户没有明确确认时，不进入下一步。
3. 支持从任意一步开始；已明确给出步骤序号时，不重复询问。
4. 用户要求修改当前结果时，只重做当前步骤，不擅自推进。
5. 重新执行较早步骤后，说明哪些后续产物需要重新生成。
6. 当前用户要求优先于工作流默认参数；工作流默认参数优先于子 Skill 的自动默认值。
7. 子 Skill 自身要求的生成前确认、安全检查和验证步骤仍然生效。
8. 只处理用户指定的文章及其产物，不改动无关文件。

## 开始工作流

先确认文章路径。找不到文章或存在多个可能文件时，询问用户，不猜测。

未指定起始步骤时，使用 `references/receipts.md` 中的开始话术，列出：

1. 去除 AI 味
2. 正文配图
3. 封面制作
4. 文章排版

让用户直接回复序号。不要执行任何步骤。

用户已经明确说“从第 2 步开始”“只做排版”或给出明确序号时，直接进入对应步骤。

## 第 1 步：去除 AI 味

使用 `$humanizer-zh`：

1. 保留原意、事实、标题结构、链接和图片引用。
2. 处理生硬表达、重复内容、模板化句式和明显的 AI 写作痕迹。
3. 不因为本工作流额外增加或删除 Emoji。
4. 默认不覆盖原稿，输出带 `_去除AI味` 后缀的 Markdown 文件。
5. 完成后检查标题层级、链接和图片引用是否仍然完整。

完成后按回执模板汇报，说明下一步是“第 2 步：正文配图”，等待用户回复 `2`。

## 第 2 步：正文配图

使用 `$baoyu-article-illustrator`：

1. 读取 `references/defaults.md` 中的正文配图参数。
2. 先分析文章结构，给出配图位置、数量和生成参数。
3. 按子 Skill 要求，在生成图片前等待用户确认。
4. 将图片保存到文章同目录的 `assets/`。
5. 使用简单文件名：`section-01.png`、`section-02.png`；同一章节多图时使用 `section-02-1.png`。
6. 将相对图片路径插入 Markdown，生成带 `_配图版` 后缀的文件。
7. 检查图片文件存在、Markdown 引用有效、图片位置与正文语义匹配。

完成后按回执模板汇报，说明下一步是“第 3 步：封面制作”，等待用户回复 `3`。

## 第 3 步：封面制作

使用 `$baoyu-cover-image`：

1. 读取 `references/defaults.md` 中的封面参数。
2. 根据文章标题、核心冲突和目标读者形成封面方案。
3. 按子 Skill 要求，在生成图片前等待用户确认。
4. 将封面保存为文章同目录下的 `assets/cover.png`。
5. 检查画面主题、文字清晰度、缩略图可读性和宽高比。

完成后按回执模板汇报，说明下一步是“第 4 步：文章排版”，等待用户回复 `4`。

## 第 4 步：文章排版

使用 `$gzh-design`：

1. 读取 `references/defaults.md` 中的排版参数。
2. 优先使用配图版 Markdown；用户指定其他版本时使用指定文件。
3. 生成纯净 HTML 和一键复制预览版 HTML。
4. 目录完整展示所有二级标题并连续编号。
5. 纯净 HTML 保留相对图片路径。
6. 一键复制预览版将本地图片内嵌，确保复制到微信公众号编辑器时能够携带图片。
7. 检查目录数量、标题数量、图片引用和排版校验结果。

完成后给出最终回执，引导用户把预览版复制到微信公众号编辑器进行最后检查。用户确认前，不宣称文章已经发布。

## 缺少子 Skill

如果 `$humanizer-zh`、`$baoyu-article-illustrator`、`$baoyu-cover-image` 或 `$gzh-design` 不可用：

1. 明确列出缺少的 Skill。
2. 不自动下载、安装或替换实现。
3. 保留已完成产物，等待用户决定。
